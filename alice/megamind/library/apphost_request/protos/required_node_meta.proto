syntax = "proto3";

package NAlice.NMegamindAppHost;

// This protobuf provides required data to run each node in MM
message TRequiredNodeMeta {
    string SpeechKitRequestId = 1;
    uint64 HypothesisNumber = 2;
    bool EndOfUtterance = 3;
}
