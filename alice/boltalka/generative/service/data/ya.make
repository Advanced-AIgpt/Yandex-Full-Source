UNION()

OWNER(g:alice_boltalka)

FROM_SANDBOX(FILE 1248982957 OUT bad_dict.txt)
FROM_SANDBOX(FILE 1248985956 OUT bpe.voc)
FROM_SANDBOX(FILE 1248988835 OUT model.npz)
FROM_SANDBOX(FILE 1248998484 OUT token_to_id.voc)

FROM_SANDBOX(FILE 1441739715 OUT boltalka-bert-mse.npz)
FROM_SANDBOX(FILE 1429944589 OUT start.trie)
FROM_SANDBOX(FILE 1429946448 OUT cont.trie)
FROM_SANDBOX(FILE 1429947801 OUT vocab.txt)

END()
