INSERT INTO {{output1}} WITH TRUNCATE
SELECT *
FROM {{input1}}
TABLESAMPLE BERNOULLI(80.0) REPEATABLE(125);
COMMIT;

INSERT INTO {{output2}} WITH TRUNCATE
SELECT left.*
FROM {{input1}} as left
LEFT ONLY JOIN {{output1}} as right
USING(reqid);