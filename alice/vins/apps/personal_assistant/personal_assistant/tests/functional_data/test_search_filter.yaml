test_not_match:
  experiments: ['search_filter_set']

  app_info:
    ignore:
      - watch
      - dexp
      - irbis
      - speaker
      - navigator_android
      - navigator_ios

  dialog:
    алиса можешь не открывать поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    а давай без поиска придумай что нибудь сама:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как включить безумный режим:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    я уже маленькая но мне же нельзя я когда маленькая мне нужна:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    алиса не надо поиск открывать мне уже:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    не надо мне поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    зачем поиск открывать:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    алиса надо сама и не поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как сделать поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    не надо никакого поиска:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как убрать масштабирование с браузера яндекс:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    а какой самый лучший парк алиса поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    алиса хватит открывать поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как то не получается такое положение найти:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    куда куда не надо никакой поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    отключи поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    включи сказочный мир поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как удалить из тебя данные поиска:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как включить полноэкранный режим:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    ты должна очистить поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    алиса включи экономный режим:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    мне не надо поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    алиса я тебя не просила открывать поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    не надо поисков:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    скажи пожалуйста как настроить кроссворды:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    а ты так не можешь открыть да и посмотреть да:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как стать фокусником открывай поиск:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как удалить фото с алисой:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как то не слишком развернутая:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    как удалить всю музыку из вк через телефон:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    алиса как надо удалить текст:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'
    не хочу я видеть пожалуйста сотри их:
      vins_form:
        name_regexp: '^((?!(search_filter)).)*$'

    какой режим поиска:
      vins_form:
        name_regexp: '^.*search_filter.*$'
    как включить режим поиска без ограничений:
      vins_form:
        name_regexp: '^.*search_filter.*$'
    а где режим поиска:
      vins_form:
        name_regexp: '^.*search_filter.*$'
