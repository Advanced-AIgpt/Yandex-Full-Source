personal_assistant.general_conversation.general_conversation:
  - filename: abort_fix_list.txt
    add_word_boundaries: true
    add_arbitrary_prefix: true
    add_arbitrary_suffix: true

  - filename: drugs_short_fix_list.txt
    add_word_boundaries: true
    add_arbitrary_prefix: true
    add_arbitrary_suffix: true

  - filename: provocation_fix_list.txt
    add_word_boundaries: true
    add_arbitrary_prefix: true
    add_arbitrary_suffix: true

  - filename: sex_fix_list.txt
    add_word_boundaries: true
    add_arbitrary_prefix: true
    add_arbitrary_suffix: true

  - filename: regex_classes.txt
    add_word_boundaries: false
    add_arbitrary_prefix: true
    add_arbitrary_suffix: true

  - filename: from_dialog_638.txt
    add_word_boundaries: true
    add_arbitrary_prefix: true
    add_arbitrary_suffix: true
