убери '@navi_map_layers(5)'(layer)
выключи '@navi_map_layers(5)'(layer)
скрой '@navi_map_layers(5)'(layer)
скрой '@navi_map_layers(5)'(layer) на карте
спрячь '@navi_map_layers(5)'(layer)
выключи слой '@navi_map_layers(5)'(layer)
выключи слой '@navi_map_layers(5)'(layer) на карте
убери слой '@navi_map_layers(5)'(layer)
убери '@navi_map_layers(5)'(layer) на карте
убери '@navi_map_layers(5)'(layer) с карты
убери слой '@navi_map_layers(5)'(layer) с карты
выключи '@navi_map_layers(5)'(layer) на карте
не показывай '@navi_map_layers(5)'(layer)
выруби '@navi_map_layers(5)'(layer)
сбрось '@navi_map_layers(5)'(layer)
очисть карту от '@navi_map_layers(5:gen)'(layer)

выключи 'спутник'(layer)
выключи 'вид со спутника'(layer)
выключи слой 'спутник'(layer)
убери 'карту со спутника'(layer)

'парковку'(layer) выключи
без 'пробок'(layer)
отключить 'пробки'(layer)
отменить 'пробки'(layer)
скрыть 'пробки'(layer) по времени до
выключи 'пробки'(layer)
скорой 'парковки'(layer)
убрать 'камеры'(layer)
отключи 'пробки'(layer)
скрыть 'парковки'(layer)
слушай яндекс убери 'пробки'(layer)
больше не хочу 'спутниковую карту''(layer)
очисть карту от 'пробок'(layer)
не нужно 'пробок'(layer) на карте
