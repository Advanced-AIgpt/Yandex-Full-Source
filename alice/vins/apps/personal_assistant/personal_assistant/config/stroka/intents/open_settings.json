
{
  "form": "open_settings",
  "events": [{
    "event": "submit",
    "handlers": [{
      "handler": "callback",
      "name": "nlg_callback",
      "params": {
        "phrase_id": "render_result"
      }
    }, {
      "handler": "callback",
      "name": "open_settings"
    }]
  }],
  "slots": [{
    "slot": "target",
    "type": "settings",
    "optional": true,
    "events": []
  }]
}
