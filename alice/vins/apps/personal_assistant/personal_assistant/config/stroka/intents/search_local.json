
{
  "form": "search_local",
  "events": [{
    "event": "submit",
    "handlers": [{
      "handler": "callback",
      "name": "nlg_callback",
      "params": {
        "phrase_id": "render_result"
      }
    }, {
      "handler": "callback",
      "name": "search_local"
    }]
  }],
  "slots": [{
    "slot": "query",
    "type": "string",
    "optional": true,
    "events": []
  }]
}

