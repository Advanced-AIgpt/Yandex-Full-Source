
{
  "form": "open_flash_card",
  "events": [{
    "event": "submit",
    "handlers": [{
      "handler": "callback",
      "name": "nlg_callback",
      "params": {
        "phrase_id": "render_result"
      }
    }, {
      "handler": "callback",
      "name": "open_flash_card"
    }]
  }],
  "slots": []
}
