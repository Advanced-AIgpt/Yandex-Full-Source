
{
  "form": "open_disk",
  "events": [{
    "event": "submit",
    "handlers": [{
      "handler": "callback",
      "name": "nlg_callback",
      "params": {
        "phrase_id": "render_result"
      }
    }, {
      "handler": "callback",
      "name": "open_disk"
    }]
  }],
  "slots": [{
    "slot": "disk",
    "type": "disk",
    "optional": true,
    "events": []
  }]
}
