
{
  "form": "mute",
  "events": [{
    "event": "submit",
    "handlers": [{
      "handler": "callback",
      "name": "nlg_callback",
      "params": {
        "phrase_id": "render_result"
      }
    }, {
      "handler": "callback",
      "name": "mute"
    }]
  }],
  "slots": []
}
