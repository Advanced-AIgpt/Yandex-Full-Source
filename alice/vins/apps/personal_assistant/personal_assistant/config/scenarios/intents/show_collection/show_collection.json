{
  "form": "show_collection",
  "events": [
    {
      "event": "submit",
      "handlers": [
        {
          "handler": "callback",
          "name": "universal_callback"
        }
      ]
    }
  ],
  "slots": [{
    "slot": "collection",
    "types": ["collection", "string"],
    "optional": false,
    "events": [
      {
        "event": "ask",
        "handlers": [
          {
            "handler": "callback",
            "name": "nlg_callback",
            "params": {
              "phrase_id": "ask_collection"
            }
          }
        ]
      }
    ]
  }]
}
