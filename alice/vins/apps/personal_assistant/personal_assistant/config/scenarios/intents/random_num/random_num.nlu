# fixlist
загадай число от 'одного'(count_from) до 'одного'(count_to)

можешь пожалуйста любое число загадать
а можешь рандомное число загадать
загадать произвольное число
можешь мне случайное число назвать

# empty form
скажи число
загадай число
скажи какое-нибудь число
скажи любое число
выбери цифру
загадай случайное число

# templates
можешь загадать число от '@int_number(4)'(count_from) до '@int_number(3)'(count_to)
выбери число от '@int_number(10)'(count_from) до '@int_number(10)'(count_to)
загадай мне число от '@int_number(10)'(count_from) до '@int_number(10)'(count_to)
число между  '@int_number(10)'(count_from) и  '@int_number(10)'(count_to)
число от 'одного'(count_from) до 'ста'(count_to)
выбери число от 'ста пятнадцати'(count_from) до 'трехсот миллионов'(count_to)
от '@int_number(10)'(count_from) до '@int_number(10)'(count_to) скажи число
выбери число между '@int_number(10)'(count_from) и '@int_number(10)'(count_to)
скажи случайное число от '@int_number(10)'(count_from) до '@int_number(10)'(count_to)
мне нужно одно число от '@int_number(10)'(count_from) до '@int_number(10)'(count_to)
выбери число между '@int_number(10)'(count_from) и '@int_number(10)'(count_to)
скажи число c '@int_number(10)'(count_from) до '@int_number(10)'(count_to)
дай число от '@int_number(10)'(count_from) до '@int_number(10)'(count_to)
скажи любое число до '@int_number(10)'(count_to)
назови любую цифру до '@int_number(10)'(count_to)
назови цифру от '@int_number(10)'(count_from) до '@int_number(10)'(count_to)
скажи любое число больше '@int_number(10)'(count_from)
скажи любое число меньше '@int_number(10)'(count_to)
загадать число до '@int_number(10)'(count_to)

# logs
алиса выдай случайное число
алиса кинь кости
алиса подбрось монетку еще раз
