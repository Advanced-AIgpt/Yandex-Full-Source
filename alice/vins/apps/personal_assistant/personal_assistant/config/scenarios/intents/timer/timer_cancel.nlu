@cancel(10) таймер
@cancel(10) таймера
@cancel(10) таймеры
давай таймер @cancel(10)
таймер @cancel(10)

@cancel(all) таймер '@sleep_specification(all)'(specification)
@cancel(all) таймеры '@sleep_specification(all)'(specification)
@cancel(all) '@sleep_specification(all)'(specification) таймер
давай таймер '@sleep_specification(all)'(specification) @cancel(10)
таймер '@sleep_specification(all)'(specification) @cancel(10)

текущий таймер @cancel(10)
@cancel(10) текущий таймер

@cancel(10) 'все'(timer_id) таймеры
'все'(timer_id) таймеры @cancel(10)

@cancel(10) таймер на 'секунду'(time)
@cancel(10) таймер на 'одну секунду'(time)
@cancel(10) таймер на 'две секунды'(time)
@cancel(10) таймер на 'три секунды'(time)

@cancel(10) таймер на 'минуту'(time)
@cancel(10) таймер на 'одну минуту'(time)
@cancel(10) таймер на 'две минуты'(time)
@cancel(10) таймер на 'три минуты'(time)

@cancel(10) таймер на 'час'(time)
@cancel(10) таймер на 'один час'(time)
@cancel(10) таймер на 'два часа'(time)
@cancel(10) таймер на 'три часа'(time)

@cancel(5) таймер на '@int_number(2:acc) секунд'(time)
@cancel(5) таймер на '@int_number(2:acc) минут'(time)
@cancel(5) таймер на '@int_number(2:acc) часов'(time)
таймер на '@int_number(2:acc) секунд'(time) @cancel(5)
таймер на '@int_number(2:acc) минут'(time) @cancel(5)
таймер на '@int_number(2:acc) часов'(time) @cancel(5)

@cancel(5) пожалуйста таймер на '@int_number(2:acc) минут'(time)
@cancel(5) пожалуйста таймер на '@int_number(2:acc) секунд'(time)
@cancel(5) пожалуйста таймер на '@int_number(2:acc) часов'(time)

@cancel(5) таймер пожалуйста на '@int_number(2:acc) минут'(time)
@cancel(5) таймер пожалуйста на '@int_number(2:acc) секунд'(time)
@cancel(5) таймер пожалуйста на '@int_number(2:acc) часов'(time)
таймер пожалуйста на '@int_number(2:acc) минут'(time) @cancel(5)
таймер пожалуйста на '@int_number(2:acc) секунд'(time) @cancel(5)
таймер пожалуйста на '@int_number(2:acc) часов'(time) @cancel(5)

@cancel(2) таймер на '@int_number(2:acc) минут @int_number(2:acc) секунд'(time)
@cancel(2) таймер на '@int_number(2:acc) часов @int_number(2:acc) секунд'(time)
@cancel(2) таймер на '@int_number(2:acc) часов @int_number(2:acc) минут'(time)
@cancel(2) таймер на '@int_number(2:acc) часов и @int_number(2:acc) минут'(time)
@cancel(2) таймер на '@int_number(2:acc) часов @int_number(2:acc) минут @int_number(2:acc) секунд'(time)
@cancel(2) таймер на '@int_number(2:acc) часов @int_number(2:acc) минут и @int_number(2:acc) секунд'(time)

таймер на '@int_number(2:acc) минут @int_number(2:acc) секунд'(time) @cancel(2)
таймер на '@int_number(2:acc) часов @int_number(2:acc) секунд'(time) @cancel(2)
таймер на '@int_number(2:acc) часов @int_number(2:acc) минут'(time) @cancel(2)
таймер на '@int_number(2:acc) часов и @int_number(2:acc) минут'(time) @cancel(2)
таймер на '@int_number(2:acc) часов @int_number(2:acc) минут @int_number(2:acc) секунд'(time) @cancel(2)
таймер на '@int_number(2:acc) часов @int_number(2:acc) минут и @int_number(2:acc) секунд'(time) @cancel(2)

@cancel(2) таймер на '@int_number(2:acc) @int_number(2:acc)'(time)
алиса @cancel(2) таймер на '@int_number(2:acc) @int_number(2:acc)'(time)
давай ка @cancel(2) таймер на '@int_number(2:acc) @int_number(2:acc)'(time)

@cancel(5) таймер на 'минуту @int_number(5:acc) секунд'(time)
@cancel(5) таймер на 'минуту @int_number(5:acc)'(time)

стоп сбросить таймер
стоп таймер
сбросить таймер
можешь @cancel(10) таймер
можешь таймер @cancel(10)

# from logs
очисть таймер
#todo: удали i-тый таймер

# DIALOG-7866, ALICE-14596
стоп таймер
алиса стоп таймер
таймер стоп
алиса таймер стоп
стоп сбросить таймер
сбросить таймер
