syntax = "proto3";

package NAlice.NHollywood;

import "alice/megamind/protos/common/frame.proto";

// state of the session (basically the whole frame)
message TRemindersState {
    TSemanticFrame SemanticFrame = 1 [json_name = "semantic_frame"];
}
