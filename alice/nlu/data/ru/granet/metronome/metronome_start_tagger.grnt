import: metronome/common.grnt

form alice.metronome.start.tagger:
    slots:
        bpm:
            source: $Metronome.BPMNumber
            type: sys.num

    root:
        [.* $Metronome.BPMNumber*]
