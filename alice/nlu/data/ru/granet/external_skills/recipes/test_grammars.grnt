import: external_skills/recipes/entities/ingredient.grnt

# грамматики, используемые только в тестах для проверки, что все сущности выделяются правильно

form alice.recipes.test.ingredient:
    slots:
        ingredient:
            type:
                Ingredient
                string
            source: $Ingredient
    root:
        $Ingredient
