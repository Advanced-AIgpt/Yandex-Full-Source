import: zen/zen_search_common.grnt

form alice.zen_search:
    slots:
        search:
            type: string
            source: $SearchSlot
    root:
        $Root
$Root:
    [$SearchPhrease+ $Zen+ $SearchQuery* $Search]
filler:
    $Filler

