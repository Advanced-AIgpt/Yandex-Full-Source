import: zen/zen_search_common.grnt

form alice.zen_context_search:
    slots:
        search:
            type: string
            source: $SearchSlot
    root:
        $Root
$Root:
    [$SearchPhrease* $Zen* $SearchQuery* $SearchSlot]
filler:
    $Filler