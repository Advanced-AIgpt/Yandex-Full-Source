import: translate/common.grnt

form personal_assistant.scenarios.translate.translation__ellipsis:
    slots:
        to_language:
            type:
                string
            source: $ToLanguage

    root:
        а (как|$Show)? $ToSpec
        [а $ToSpec (как|$Show)]

    filler:
        будет
