form alice.scenarios.get_weather_pressure:
    enable_alice_tagger: true
    enable_granet_parser: false
    slots:
        when:
            type:
                sys.datetime_range
                sys.datetime
        day_part:
            type:
                custom.day_part
        where:
            type: string
