recommendedConcertEvents {
    items {
        event {
            argument
            title
            contentRating
            image {
                image(size: $imageSize) {
                    url
                }
            }
        }
        scheduleInfo {
            placePreview
            preview(short: $shortDate) {
                text
            }
        }
    }
}
