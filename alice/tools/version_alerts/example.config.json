{
  "nannyServiceId": {
    "url": "Url to get data",
    "token_variable": "Environment variable with OAuth token (optional)",
    "beta_for": "Production service id (optional)",
    "check_resources": ["List of resources to check with prod"],
    "check_resources_only": false,
    "params": {
      "foo": "bar"
    },
    "connect_timeout": "Connection timeout",
    "parser": "external parser to handle result"
  }
}
